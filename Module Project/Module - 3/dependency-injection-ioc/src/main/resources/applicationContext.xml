<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Service Beans -->
    <bean id="emailService" class="com.example.EmailMessageService"/>
    <bean id="smsService" class="com.example.SMSMessageService"/>
    <bean id="whatsAppService" class="com.example.WhatsAppMessageService"/>

    <!-- Backward compatibility default bean -->
    <bean id="messageService" class="com.example.EmailMessageService"/>

    <!-- 1. Setter Injection Bean (Singleton Scope by default) -->
    <bean id="messagePrinterSetter" class="com.example.MessagePrinter"
          init-method="init" destroy-method="cleanup" scope="singleton">
        <property name="service" ref="emailService"/>
        <property name="clientName" value="Enterprise Email Dispatcher"/>
    </bean>

    <!-- 2. Constructor Injection Bean -->
    <bean id="messagePrinterConstructor" class="com.example.MessagePrinter">
        <constructor-arg ref="whatsAppService"/>
        <constructor-arg value="Mobile Alerts Broadcaster"/>
    </bean>

    <!-- 3. Prototype Scope Bean (New instance per getBean call) -->
    <bean id="messagePrinterPrototype" class="com.example.MessagePrinter" scope="prototype">
        <property name="service" ref="smsService"/>
        <property name="clientName" value="Emergency SMS Gateway"/>
    </bean>

    <!-- Default messagePrinter alias for legacy code compatibility -->
    <alias name="messagePrinterSetter" alias="messagePrinter"/>

</beans>